import { useEffect } from 'react';

const Home = () => {
    useEffect(() => {
        const fetchRecipes = async () => {
            const response = await fetch(
                
            )
        }
    })

    return (
        <div className="home">
            <div className="recipes">

            </div>
        </div>
    )
}

export default Home;